void	ft_clear_lst_cmd_previous(t_cmd **cmd)
{
	t_cmd	*previous;

	if (!cmd || !*cmd)
		return ;
	previous = (*cmd)->previous;
	ft_cmd_del(*cmd);
	ft_clear_lst_cmd_previous(&previous);
}

void	ft_clear_lst_cmd_next(t_cmd **cmd)
{
	t_cmd	*next;

	if (!cmd || !*cmd)
		return ;
	next = (*cmd)->next;
	ft_cmd_del(*cmd);
	ft_clear_lst_cmd_next(&next);
}

TODO:

creat dir for srcs

make a better prompt
uppdate SHLVL 
here_doc function
make a lst->char** func for env



make a multi join


Use tty function for check if is atty and open tty
isatty, ttyname, ttyslot, 

use ioctl to send request to term
ioctl

use termios function to formate tty io
tcsetattr, tcgetattr
 
termcap functin does the same but more complicated ?
tgetent, 
tgetflag, tgetnum, tgetstr, tgoto, 
tputs

termcap -> termios -> curse -> ncurse


DONE:
/*	TODO: sigQUIT DOES NOTHING	actually it move cursor ??*/
/*	TODO:	handle exit code	*/
/*	TODO:	prevent exit newline especially in child	*/
/*	TODO: handle cmd null better; */
handle erro gestion cmd in child ??????? 


